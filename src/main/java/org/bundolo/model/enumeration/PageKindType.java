package org.bundolo.model.enumeration;

public enum PageKindType {
	home, forum, contests, events, user, texts, lists, connections, news, serials, authors, about, contact, custom, help;

	@Override
	public String toString() {
		return super.toString();
	}
}
