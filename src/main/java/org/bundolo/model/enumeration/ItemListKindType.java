package org.bundolo.model.enumeration;

public enum ItemListKindType {
    // be careful when editing, item lists are sorted by these
    general, elected, personal;

    @Override
    public String toString() {
	return super.toString();
    }
}